# multiplication-practice

An interactive, console-driven mathematics evaluation engine built in Python. Features automated pseudo-random problem generation with structural bounds, integrated try-except input validation modules to prevent terminal crashes, and a persistent context-managed I/O loop that logs user performance metrics directly to a local disk leaderboard.